# Installed inside the image by `bun install`.
node_modules

# Mounted as a volume at run time.
logs
*.log

.git
.gitignore
.claude
Dockerfile
docker-compose.yml
README.md

# dotenv environment variable files - hold live credentials, injected at
# runtime via docker-compose's env_file, never baked into the image
.env*
